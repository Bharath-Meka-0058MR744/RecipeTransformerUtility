{
  "id": "string (UUID)",
  "name": "string",
  "label": "string",
  "description": {
    "overview": "string",
    "details": "string (markdown supported)"
  },
  "version": "string (semver)",

  "tags": {
    "category": ["string"],
    "availableOn": ["string"],
    "keyword": ["string"]
  },

  "provenance": { 
    "status": "string"
  },

  "dependencies": {
    "applications": [{
      "id": "string",
      "label": "string",
      "icon": "string"
    }]
  },

  "compatibility": {},

  "configurations": {},

  "usageStatistics": {}
}
